import { ReactElement,ReactNode } from "react";
import './index.less'

const AboutView = () : ReactElement<ReactNode> => {
    return (
        <div className="about-view"></div>
    )
};

export default AboutView;
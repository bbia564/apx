import { ReactElement,ReactNode } from "react";
import './index.less'

const ResearchView = () : ReactElement<ReactNode> => {
    return (
        <div className="research-view"></div>
    )
};

export default ResearchView;
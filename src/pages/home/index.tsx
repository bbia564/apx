import { ReactElement,ReactNode } from "react";
import './index.less'

const HomeView = () : ReactElement<ReactNode> => {
    return (
        <div className="home-view"></div>
    )
};

export default HomeView;
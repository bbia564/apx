import { ReactElement,ReactNode } from "react";
import './index.less'

const FundsView = () : ReactElement<ReactNode> => {
    return (
        <div className="funds-view"></div>
    )
};

export default FundsView;
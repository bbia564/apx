import { ReactElement,ReactNode } from "react";
import './index.less'

const InsightsView = () : ReactElement<ReactNode> => {
    return (
        <div className="insights-view"></div>
    )
};

export default InsightsView;